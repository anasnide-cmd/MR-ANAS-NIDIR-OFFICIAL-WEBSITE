import FundClient from '../../components/Fund/FundClient';

export const metadata = {
    title: 'Support Us | Mr Build',
    description: 'Fuel the vision and support the development.',
};

export default function FundPage() {
    return <FundClient />;
}
